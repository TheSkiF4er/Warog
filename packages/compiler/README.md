# @warog/compiler

Базовый компилятор **Warog**, обрабатывающий файлы `.wg.tsx` и генерирующий:

- SSR HTML,
- JSON-артефакт состояния,
- клиентский модуль.

В версии 1.0.0 реализация минимальна и служит стартовой точкой.
